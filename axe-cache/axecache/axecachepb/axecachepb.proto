syntax = "proto3";

package axecachepb;

message Request {
    string name = 1;
    string key = 2;
}

message Response {
    bytes value = 1;
}

service AxeCache {
    rpc Get(Request) returns (Response);
}
